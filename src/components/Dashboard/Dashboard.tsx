import * as React from 'react';

export default
class Dashboard extends React.Component {
  render(): JSX.Element {
    return (
      <div style={{ width: '500px', minHeight: '100%', backgroundColor: 'cyan' }} >
      </div>
    );
  }
}
